
tables:
    googleanalytics_by_date:
        './db/parquet/googleanalytics_by_date'
    googleanalytics_by_date_site:
        './db/parquet/googleanalytics_by_date_site'
    googleanalytics:
        './db/json/dataset*/*'

parameters:
    site: '%SITE%' #(lon|nyc)
    date: '%DATE%' #[0-9]{4}-[0-9]{2}-[0-9]{2}
